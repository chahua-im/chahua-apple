# Vendor

Third-party source and binaries vendored into this repo.

Each library gets its own subdirectory with `LICENSE`, `VERSION`, and upstream source.

Prefer Swift Package Manager for dependencies when possible. Use this folder for patched forks, non-SPM C/C++ code, or binary xcframeworks (`Vendor/Binaries/`).
